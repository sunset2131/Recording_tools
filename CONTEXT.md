# 录制工具领域术语

## 证据包（Evidence Package）

一次录制产生的、可独立复盘用户行为及运行环境的文件集合。它是本项目的主要产物，不等同于可直接执行的自动化测试脚本。

## 原始事件（Raw Event）

录制器直接观察到的单次浏览器或页面事件，例如一次输入变化、点击、网络请求或控制台错误。原始事件用于审计和排查，不保证适合直接生成测试步骤。

## 逻辑步骤（Logical Step）

将相关原始事件按时间、页面和目标元素归并后形成的可读操作单元，例如把连续键入归并为一次最终值 `fill`。它是人工和 AI 理解操作流程的主要视图。

## 页面状态（Page State）

某个逻辑步骤前后页面在特定时间点的可复盘状态，包含 URL、标题、DOM 快照和截图，并可关联网络与控制台事件。

## 定位候选（Locator Candidate）

针对操作目标生成的一组可能的元素定位方式。每个候选包含定位类型、表达式、唯一性和稳定性评分；评分是建议排序，不是成功保证。

## 证据完整性（Evidence Integrity）

证据包索引、引用文件和日志之间的一致程度。完整证据包的索引可解析、引用文件存在且写入成功；否则必须标记为不完整并说明警告。

## 检查点（Checkpoint）

录制人员对某一步填写的业务预期及验证方式。本次升级不实现检查点，只预留后续扩展边界。

## 录制上下文（Recording Context）

一次录制使用的隔离浏览器环境及其页面、框架、网络和控制台事件集合。它不等同于用户已有的浏览器配置，也不自动携带已有登录态。

## 部分证据包（Partial Evidence Package）

因浏览器崩溃、写入失败或停止超时而未通过完整性校验，但仍保留部分可读取证据的录制产物。它必须明确标记为不完整，不能当作成功录制。

## 状态稳定等待（State Settling）

操作发生后，为等待导航、SPA 路由、网络空闲或异步渲染完成而执行的有限等待过程。等待原因和实际耗时属于页面状态证据的一部分。

## 证据保留策略（Evidence Retention Policy）

对录制目录及其中未脱敏输入值的生命周期管理约定。本项目不自动上传或删除，由使用组织自行归档、传输和清理。

## 证据状态（Evidence State）

逻辑步骤操作前或操作后采集的独立页面快照。每个状态拥有自己的状态 ID、时间、URL、标题、截图、DOM、稳定等待信息和截断元数据；内容相同也不复用状态 ID。

## 步骤窗口（Step Window）

用于关联异步网络和控制台事件的逻辑步骤时间范围，从步骤开始及其前状态采集起，到后状态稳定采集完成为止。一个网络事件可跨越多个步骤窗口，因此通过 `stepIds` 多对多关联。

## 有效配置快照（Effective Configuration Snapshot）

录制开始前完成校验并被冻结的配置副本。它写入 `evidence.json`，用于解释本次录制中的响应体、DOM、Trace、ZIP 和网络过滤行为；录制进行时不得变化。
